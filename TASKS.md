# 📋 Project Tasks (Visitor Host Revolution 2.0)

## 📌 今後の開発タスク & メモ (Upcoming Tasks)

### 1. GAS による PUSH 型リアルタイム同期設定
- **概要**: Google フォーム送信時・スプレッドシート更新時のイベントから `https://revo.k-d-o.biz/api/sync.php` をWebフック呼び出し（PUSH型）して SQLite DB に即時同期する。
- **状況**: GAS スクリプト作成済。スプレッドシート側へのトリガー設置手順案内済。

### 3. [フェーズ1] 安全なリファクタリングのためのコンポーネントカタログ（プレビュー環境）の構築
- **概要**: 既存のプロダクトコード（各モーダルやビュー）を変更する前に、全ての共通パーツ（ボタン、フォーム、モーダル、カード等）を一堂に集約して表示・動作確認できる「コンポーネントカタログ・ページ（`ViewCatalog.html`）」を作成・整備する。
- **目的**: 既存機能への影響をゼロに抑え（Gitでの安全なロールバックを担保）、デザインのズレや崩れを視覚的に一発で検知できる環境を構築することで、リファクタリングの作業効率を最大化する。
- **状況**: `ViewCatalog.html` の初版雛形（Atoms / Molecules / Organisms）を新規作成完了。

